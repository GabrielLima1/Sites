<%= simple_form_for @cat_ctrl_qualit, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :ccquality_name %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                cat_ctrl_qualits_path, :class => 'btn btn-default' %>
<% end %>
